name: Refresh dependencies target

on:
  push:
    branches: [ master ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: git branch -a
      - run: git branch dependency-updates
      - run: git push origin dependency-updates
